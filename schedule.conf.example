# 1 = enabled, 0 = disabled
ENABLED="1"

# Allowed days as numbers (locale-independent): 1=Mon ... 7=Sun
# Examples:
#   Mon-Fri: "1 2 3 4 5"
#   Daily:   "1 2 3 4 5 6 7"
DAYS_NUM="1 2 3 4 5 6 7"

# Time window (24h HH:MM)
START_TIME="08:00"
STOP_TIME="18:00"

# If START_TIME > STOP_TIME, the window crosses midnight (e.g. 20:00 -> 06:00).
# After midnight, the day check uses "yesterday" to match the intended schedule.